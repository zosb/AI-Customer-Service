# AI 智能客服系统——运行指南

本文以 Windows 10/11 + CMD 为例。项目默认使用本地 Ollama，不需要 OpenAI/通义千问 API Key。

## 1. 环境要求

- Python 3.11
- Node.js `^22.22.2`、`^24.15.0` 或 `>=26.0.0`（推荐 Node 24.15+）
- MySQL 8.x
- Ollama
- Git（提交 GitHub 时需要）

确认版本：

```cmd
python --version
node --version
npm --version
mysql --version
ollama --version
git --version
```

## 2. 配置环境变量

项目根目录：

```cmd
copy .env.example .env
```

至少修改：

```text
MYSQL_PASSWORD=至少12位ASCII强密码（大写+小写+数字+英文特殊符号）
JWT_SECRET_KEY=一段足够长的随机字符串
```

`.env` 是本机私密配置，不得提交 Git。

前端如需修改 API 地址：

```cmd
copy frontend\.env.example frontend\.env.local
```

默认后端地址为 `http://127.0.0.1:8000`。

## 3. 准备 Ollama 模型

```cmd
ollama pull qwen3.5:4b
ollama pull qwen3-embedding:0.6b
ollama list
```

检查服务：

```cmd
curl http://127.0.0.1:11434/api/version
```


## 4. 后端安装

```cmd
cd backend
python -m venv .venv
call .venv\Scripts\activate.bat
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

安装完成后做一次真实 Ollama 预检：

```cmd
python scripts\check_ollama.py
```

该检查会验证 Ollama 服务、`qwen3.5:4b`、`qwen3-embedding:0.6b`，并实际执行一次 1024 维 Embedding 与一次 Chat 请求。全部通过后再继续初始化数据库和启动 Web 服务，可提前排除模型未下载、Ollama 未启动或模型调用失败等问题。

## 5. 初始化 MySQL

先确保根目录 `.env` 中的 MySQL 配置正确。

### 推荐方式：项目脚本 + Alembic

```cmd
python scripts\bootstrap_mysql.py
python -m alembic upgrade head
```

- `bootstrap_mysql.py`：创建数据库和项目账号，并检查读写权限。
- `alembic upgrade head`：创建/升级业务表。

提交中同时提供 `backend/database/schema.sql` 和 `backend/database/seed.sql`，便于面试官直接审阅建表语句和初始数据；实际维护以 Alembic 迁移为准。

检查数据库：

```cmd
python scripts\check_settings.py
python scripts\check_database.py
python scripts\check_business_schema.py
```

## 6. 启动后端

在 `backend` 目录、虚拟环境已激活时：

```cmd
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

访问：

- 健康检查：`http://127.0.0.1:8000/api/v1/health`
- Swagger：`http://127.0.0.1:8000/docs`
- OpenAPI：`http://127.0.0.1:8000/openapi.json`

## 7. 创建管理员

先通过前端注册一个普通账号，再执行：

```cmd
cd backend
call .venv\Scripts\activate.bat
python scripts\manage_admin.py list
python scripts\manage_admin.py grant --user-id <USER_ID>
```

## 8. 安装并启动前端

新开一个 CMD：

```cmd
cd frontend
npm ci
npm run dev -- --host 127.0.0.1
```

浏览器访问：

```text
http://127.0.0.1:5173
```

主要页面：

- `/`：AI 智能客服
- `/login`：登录
- `/register`：注册
- `/knowledge`：知识库管理
- `/admin`：运营管理后台
- `/admin/agent`：AI Agent Planner

## 9. 推荐演示流程

1. 注册/登录。
2. 管理员创建知识库并填写路由关键词。
3. 上传 `.txt` / `.md` / `.pdf` 文档，观察状态变为 `ready`。
4. 新建会话并提出知识库相关问题。
5. 观察逐步出现的 SSE 回答。
6. 展开知识来源，确认文档名称和片段摘要。
7. 对 AI 回答点赞/点踩并填写反馈。
8. 查看历史会话。
9. 管理员查看统计后台。
10. 演示多知识库自动路由和 Agent Planner。

## 10. 提交前质量检查

后端：

```cmd
cd backend
call .venv\Scripts\activate.bat
python -m pytest -q
```

前端：

```cmd
cd frontend
npm run type-check
npm run test:unit -- --run
npm run lint
npm run build
```

还应人工验证一次：上传文档 → ready → 提问 → SSE → 来源 → 历史 → 反馈 → 删除文档后向量同步清理。

## 11. 常见问题

### Ollama 在 Python 中返回 502，但 curl 正常

部分 VPN/代理会接管 localhost 请求。本项目访问本地 Ollama 时使用不继承系统代理的客户端策略。仍有问题时检查 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`、`NO_PROXY`。

### MySQL 连接失败

检查 `.env` 中 Host、Port、User、Password、Database；再运行：

```cmd
python scripts\check_settings.py
python scripts\check_database.py
```

### Chroma 或上传目录不存在

项目会在运行时创建必要目录；仓库只保留空目录占位，不提交本机向量数据或上传文档。