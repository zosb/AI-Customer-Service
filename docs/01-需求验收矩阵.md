# 需求验收矩阵

本矩阵按笔试题逐项对应当前代码。状态含义：

- **已实现**：仓库存在完整生产代码及相应测试/验证入口。
- **提交前复验**：功能已实现，但正式提交前仍建议在目标 Windows + MySQL + Ollama 环境执行全量回归。

| ID | 笔试要求 | 当前实现证据 | 状态 |
|---|---|---|---|
| AUTH-01 | 手机号或邮箱注册/登录 | `auth.py` + `AuthService` + JWT + Argon2 | 已实现 |
| SESSION-01 | 独立 Session、历史列表/详情 | `chat/sessions` API + MySQL 会话/消息表 | 已实现 |
| FEEDBACK-01 | 点赞/踩 + 可选文字反馈 | `message_feedback` + PUT/DELETE Feedback API | 已实现 |
| KB-01 | 上传 txt/md/pdf、解析、向量化、状态 | Parser/Chunk/Embedding/Chroma Pipeline + 文档状态 | 已实现 |
| KB-02 | 删除文档同步清除向量 | Knowledge lifecycle 删除服务同时处理 Chroma/DB/磁盘 | 已实现 |
| RAG-01 | 检索→Prompt→LLM→流式返回 | `StreamingChatAnswerService` + `RAGPromptBuilder` | 已实现 |
| SOURCE-01 | 展示文档名 + 片段摘要来源 | `message_sources` + Sources API + 前端来源展示 | 已实现 |
| CHAT-01 | 最近 N 轮多轮上下文 | `CONTEXT_HISTORY_ROUNDS` + history prompt | 已实现 |
| RULE-01 | 单次问题 ≤500 字 | Pydantic `max_length=500` +服务层校验 | 已实现 |
| RULE-02 | 每用户每日 ≤100 次且可配置 | `daily_question_usage` + `DAILY_QUESTION_LIMIT` | 已实现 |
| RAG-02 | 空检索标准兜底、不编造 | 服务端 fallback + `EMPTY_RETRIEVAL_REPLY` | 已实现 |
| SSE-01 | SSE 逐步输出 | `StreamingResponse` + `delta/replace/sources/done` | 已实现 |
| ERROR-01 | LLM 超时/临时错误处理 | Ollama retry/backoff + SSE error/fallback | 已实现 |
| INTENT-01 | 意图识别并写会话记录 | `classify_intent` + `chat_messages.intent` | 已实现（加分） |
| FOLLOW-01 | 2-3 个追问建议 | `FOLLOW_UP_SUGGESTION_COUNT` +前端点击复用 | 已实现（加分） |
| ADMIN-01 | 全量会话、反馈统计、日均趋势 | `/admin/*` + Vue 管理后台 | 已实现（加分） |
| ROUTE-01 | 多知识库自动路由 | Probe + routing_description +库内最终检索 | 已实现（加分） |
| UPDATE-01 | 增量文档不破坏已有向量 | Chroma upsert/独立 vector_id +文档生命周期 | 已实现（加分） |
| CONTEXT-01 | 大量上下文规则优先、防遗漏/幻觉 | `LargeContextGuard` + `AnswerEvidenceGuard` | 已实现（加分） |
| AGENT-01 | 需求拆分微服务、并行/依赖判断 | Planner + DAG + Resource Safety + API/UI | 已实现（终极挑战） |

## 提交前复验命令

```cmd
cd backend
call .venv\Scripts\activate.bat
python -m pytest -q

cd ..\frontend
npm run type-check
npm run test:unit -- --run
npm run lint
npm run build
```